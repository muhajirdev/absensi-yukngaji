import {
  style,
  css,
  breakpoints,
  display,
  borders,
  flexbox,
  palette,
  positions,
  shadows,
  sizing,
  spacing,
  typography
} from "@material-ui/system";
import styled from "styled-components";
export default styled.div`
  ${style};
  ${borders};
  ${display};
  ${flexbox};
  ${palette};
  ${positions};
  ${shadows};
  ${sizing};
  ${spacing};
  ${typography}
`;
